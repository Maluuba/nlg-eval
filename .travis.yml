dist: trusty
sudo: true
language: python
python:
  - "3.6"
  - "2.7"
install:
  - sudo apt-get install --yes default-jdk libpython unzip
  - pip install -U -e .
  - ./setup.sh
script: python -m unittest discover .
