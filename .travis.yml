dist: trusty
sudo: false
language: python
python:
  - "3.6"
  - "2.7"
before_install:
  - sudo apt-get install --yes default-jdk unzip
install:
  - pip install -U -e .
  - ./setup.sh
script: python -m unittest discover .
